# World Cup 2026 Sweepstake

## Testing

Run the regression tests with:

```
python3 -m unittest test_render -v
```

**For every change, add a regression test.** If you fix a bug or add a feature, add a test in `test_render.py` that would have caught the regression. Tests should cover the specific behaviour that changed — not just "it runs without error".
